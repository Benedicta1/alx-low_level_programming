#include <stdio.h>
/**
 * main - prints the alphabet in lowercase
 * Return: Should return a success value
 */
int main(void)
{
	char be;

	for (be = 'a'; be <= 'z'; be++)
	{
	putchar(be);
	}
	putchar('\n');
	return (0);
}
